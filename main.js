/**
 * Return dif between a and b.
 * @param {Number} a
 * @param {Number} b
 */
function main(a, b) {
   function cat(){
      return "cat"
   }
   function _sum() {
      const c = 23;
      return c ** c;
   }

   const c = 2
   return a - b + b;

   return a - b * _sum();
}

function sum(a,b){
   return a+b;
}
